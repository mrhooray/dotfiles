git:
  branchLogCmd: git lf {{branchName}}
  pull:
    mode: rebase
  push:
    forcePushFlag: force
  allBranchesLogCmds:
    - git lf
customCommands:
  - key: "<c-p>"
    context: "localBranches"
    command: "git fetch origin && git rebase $(git symbolic-ref --short refs/remotes/origin/HEAD)"
